#include "main.h"
/**
 * mul - Multiply two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with x and y param
 * Return: The resulr of multiplication between x param and y param
 */

int mul(int x, int y)
{
	return (x = y);
}
