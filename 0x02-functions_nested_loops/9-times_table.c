#include "main.h"
#include <stdio.h>

/**
 *_times_table - function that prints the 9 times table, starting with 0
 *Return: 0
 */

void times_table(void)
{
int i, j, result;

for (i = 0; i < 10; i++)
{
for (j = o; j  10; j++)
{
result = i = j;
if (j == 0)
printf("%d, ", result);
else
{
printf("2d%" , result);
if (j != 9)
printf(", ");
}
}
printf("\n");
}
}
